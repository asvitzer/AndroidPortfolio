/*
package com.alvinsvitzer.flixbook;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.Robolectric;
import org.robolectric.RobolectricTestRunner;
import org.robolectric.annotation.Config;

import static junit.framework.Assert.assertNotNull;

*/
/**
 * Local unit test, which will execute on the development machine (host).
 * Will resume testing once Roboelectric issues are fixed -> https://github.com/robolectric/robolectric/issues/2566
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 *//*

@RunWith(RobolectricTestRunner.class)
@Config(constants = BuildConfig.class, sdk = 23)
public class MovieActivityTest {

    private MovieActivity activity;

    @Before
    public void setup()  {
        activity = Robolectric.setupActivity(MovieActivity.class);
    }

    @Test
    public void checkActivityNotNull() throws Exception {
        assertNotNull(activity);
    }


}*/
